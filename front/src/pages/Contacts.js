import React from 'react';
import Navigation from '../components/Navigation';

const Contacts = () => {
    return (
        <div>
            <Navigation />
            <h1>Contacts Page</h1>
        </div>
    );
};

export default Contacts;